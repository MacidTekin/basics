@model List<Course>

@foreach(var kurs in Model ) 
    {
        @if(kurs.isActive){
           @await Html.PartialAsync("_Course",kurs)  //kursun isActive bilgisi true olursa gösterilecek.
        }
        
    }
